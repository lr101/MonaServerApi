type: object
description: An user ranking entry with rank, points and user information
properties:
  userInfoDto:
    $ref: './userInfoDto.yaml'
  rankNr:
    type: integer
    minimum: 0
  points:
    type: integer
    minimum: 0