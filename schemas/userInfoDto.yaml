type: object
required:
  - username
  - userId
properties:
  username:
    $ref: '../parameters/shortString.yaml'
  userId:
    $ref: '../parameters/id.yaml'
  description:
    $ref: '../parameters/longString.yaml'