type: array
description: "List of groups and their respective pin changes. A group missing to a local database indicates that it was deleted"
items:
  type: object
  description: A group and its respective pin changes
  required:
    - group
    - pinsAdded
    - pinsDeleted
  properties:
    group:
      $ref: "./groupDto.yaml"
    pinsAdded:
      $ref: "./pinWithOptionalImageDto.yaml"
    pinsDeleted:
      $ref: "../parameters/id.yaml"
