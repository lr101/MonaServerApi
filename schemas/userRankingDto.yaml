description: An array of sorted users by rank and points and the current user
type: object
properties:
  items:
    type: array
    items:
      $ref: './userRankingDtoInner.yaml'
  currentUser:
    $ref: './userRankingDtoInner.yaml'